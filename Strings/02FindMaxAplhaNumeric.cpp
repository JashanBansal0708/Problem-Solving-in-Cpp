#include<iostream>

void findMax(){
    int max;
    for(int i=0; i< str.size(); i++){
        if(arr[str[i] < 'a']){
        }
    }
    std::cout << -1 << '\n';
}


int main(){
    std::string str;
    int t;
    std::cin >> t;
    while(t--){
        std::cin >> str;
        findMax(str);
    }
    return 0;
}
